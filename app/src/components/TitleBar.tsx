interface TitleBarProps {
  onSettings?: () => void;
  onTelegram?: () => void;
  showBack?: boolean;
  onBack?: () => void;
}

export function TitleBar({ onSettings, onTelegram, showBack, onBack }: TitleBarProps) {
  return (
    <div className="titlebar">
      <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
        {showBack && onBack && (
          <button className="icon-btn back-btn" onClick={onBack} title="Назад">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
              <path d="M15 18l-6-6 6-6" />
            </svg>
          </button>
        )}
        {onSettings && (
          <button className="icon-btn" onClick={onSettings} title="Настройки">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
              <circle cx="12" cy="12" r="3" />
              <path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42" />
            </svg>
          </button>
        )}
        <span className="titlebar-title">FixInet.ez Beta v1.0.0 (By Seb0g1)</span>
      </div>
      {onTelegram && (
        <div className="titlebar-actions">
          <button className="icon-btn" onClick={onTelegram} title="Telegram">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm4.64 6.8c-.15 1.58-.8 5.42-1.13 7.19-.14.75-.42 1-.68 1.03-.58.05-1.02-.38-1.58-.75-.88-.58-1.38-.94-2.23-1.5-.99-.65-.35-1.01.22-1.59.15-.15 2.71-2.48 2.76-2.69a.2.2 0 00-.05-.18c-.06-.05-.14-.03-.21-.02-.09.02-1.49.95-4.22 2.79-.4.27-.76.41-1.08.4-.36-.01-1.04-.2-1.55-.37-.63-.2-1.12-.31-1.08-.66.02-.18.27-.36.74-.55 2.92-1.27 4.86-2.11 5.83-2.51 2.78-1.16 3.35-1.36 3.73-1.36.08 0 .27.02.39.12.1.08.13.19.14.27-.01.06.01.24 0 .38z" />
            </svg>
          </button>
        </div>
      )}
    </div>
  );
}
