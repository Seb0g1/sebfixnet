[Unit]
Description=Fixnet Backend API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/sebfixnet/backend
EnvironmentFile=/opt/sebfixnet/.env
ExecStart=/opt/sebfixnet/backend/.venv/bin/uvicorn main:app --host 127.0.0.1 --port 8090
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
